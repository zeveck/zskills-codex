# Phase 1 Inventory

## Local Evidence Reviewed

- `README.md` for repository purpose, install flow, and Codex port positioning.
- `skills/` for local skill names, frontmatter conventions, and Codex-adapted runtime instructions.
- `zskills-support/` for support scripts, hooks, templates, docs, and config schema evidence.
- `plans/` for executable plan structure, phase tracking, and canary coverage.
- `reports/` for durable plan reports and verification evidence.
- `scripts/` for installer and canary test conventions.
- `canary-output/` for prior progressive run-plan canary artifacts.

## Variant Names

The review packet uses these exact variant names:

- `zskills`
- `zskills-cc`
- `zskills-codex`
- `zskills-dev`

## Evidence Gaps

Any missing local evidence must be treated as an evidence gap. This canary should not invent current facts about `zskills`, `zskills-cc`, or `zskills-dev` when the corresponding checkout, branch, report, or local notes are absent from this repository.

## Initial Observations

The repository contains strong local evidence for `zskills-codex`: Codex-native skills live under `skills/`, and shared support material lives under `zskills-support/`. The available plans and reports show how local run-plan tracking, verification, and landing evidence are expected to be recorded.

The other variants need careful evidence labeling. If future phases infer how `zskills`, `zskills-cc`, or `zskills-dev` probably behave, those inferences should be separated from directly observed local files.
